---
title: "Cloud Architecture Flow"
description: "Visualize your flow with React Flow and Astro."
---

import FlowExample from '../../../components/DocFlows/FlowIngressGroup.jsx';

# Cloud Architecture Flow

Here’s your flow diagram—only run this on the **client** so it won’t try to access `window` during SSR:

<FlowExample client:only="react" />