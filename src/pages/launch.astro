---
import Layout from '../layouts/Layout.astro';
import Navigation from '../components/Site/Navigation';
import Footer from '../components/Site/Footer';

import InspectrChecker from '../components/InspectrChecker.jsx';
---
<Layout
  title="Launch mock backend from OpenAPI | Inspectr"
  description="Spin up a mock backend from any OpenAPI with zero config."
  keywords="Inspectr launch check, webhook readiness, API readiness, pre-launch testing, webhook debugging"
  ogTitle="Launch mock backend from OpenAPI | Inspectr"
  ogDescription="Spin up a mock backend from any OpenAPI with zero config."
  robots="noindex, nofollow"
>
  <Navigation client:load />
  <InspectrChecker client:load />
  <Footer />
</Layout>
